import {
  StyleSheet,
  Dimensions,
} from 'react-native';

const styles = StyleSheet.create({
  formLabel: {
    color: 'black',
    fontSize: 15,
  },
  formInput: {
  },
  divider: {
    height: 50
  }
});

export default styles;
